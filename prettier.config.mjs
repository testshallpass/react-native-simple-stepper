/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */

const config = {
  arrowParens: 'avoid',
  singleQuote: true,
  trailingComma: 'all',
};

export default config;
